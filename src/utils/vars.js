let { Attachment, Client, Collection, RichEmbed } = require("discord.js")
let { readdirSync, readFileSync } = require("fs")
let { join } = require("path")

module.exports = { Attachment, Client, Collection, RichEmbed, readdirSync, readFileSync, join }
